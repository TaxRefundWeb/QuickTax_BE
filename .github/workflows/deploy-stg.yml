name: Deploy Staging

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show context
        run: |
          echo "Repo: $GITHUB_REPOSITORY"
          echo "SHA:  $GITHUB_SHA"
          echo "Ref:  $GITHUB_REF"
