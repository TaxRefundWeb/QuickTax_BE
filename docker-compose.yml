version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: quicktax-db
    ports:
      - "15432:5432"  # 팀 공지에 따라 15432 포트 사용
    environment:
      POSTGRES_DB: quicktax
      POSTGRES_USER: quicktax          # 👈 팀 공지에 있는 ID
      POSTGRES_PASSWORD: iH7sLbPLvMe8j!6  # 👈 팀 공지에 있는 비밀번호
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: