# Swagger / OpenAPI: stg에서만 ON
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

# Swagger UI가 가져올 설정/스펙 경로를 명시적으로 고정
# (remote configuration 실패를 끝내는 핵심)
springdoc.swagger-ui.config-url=/v3/api-docs/swagger-config
springdoc.swagger-ui.url=/v3/api-docs

# (선택) Swagger UI 기본 접속 경로를 index.html로 통일
springdoc.swagger-ui.path=/swagger-ui

# ====== (기존 DB/JPA 설정은 여기엔 두지 마라) ======
# DB/JPA는 공통 application.properties 또는 .env import 쪽에서 관리하고,
# stg에선 swagger 관련만 관리하는 게 깔끔함.
